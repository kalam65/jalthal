<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Jalthal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowNoTitle">true</item> <!-- Remove title bar -->
        <item name="android:windowActionBar">false</item> <!-- Disable default ActionBar -->
        <item name="android:windowTranslucentStatus">true</item> <!-- Make the status bar translucent -->
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- Transparent status bar -->
    </style>

    <style name="Theme.Jalthal" parent="Base.Theme.Jalthal" />
</resources>